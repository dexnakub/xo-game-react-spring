# XO Game (Java Spring Boot + React + PostgreSQL + Docker)

## 💻 Requirement
- Docker & Docker Compose
- Java 25 (local, optional)
- Node.js 20 (local, optional)

---

## 🚀 Setup & Run

```bash
git clone <your_repo_url>
cd xo-game
docker compose up --build
